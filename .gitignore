# Local .terraform directories
**/.terraform/*

# Terraform crash logs
crash.log

# Terraform variable files (often contain secrets)
*.tfvars
*.tfvars.json

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan output
*.tfplan

# CLI configuration files
.terraformrc
terraform.rc

# Backup files
*.backup
*.bak
# Terraform files
.terraform/           # Contains downloaded providers (large binaries)
*.tfstate            # State files (may contain secrets)
*.tfstate.*          # Backup state files
*.tfvars             # Variable files (may contain secrets)
.terraform.lock.hcl  # Lock file (can be regenerated)
terraform.rc         # Local CLI config

git_update
